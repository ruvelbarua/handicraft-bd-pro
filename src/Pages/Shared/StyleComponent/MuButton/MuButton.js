import Button from '@mui/material/Button';
import { styled } from '@mui/system';

const MuButton = styled(Button)({
    background: 'linear-gradient(90deg, #243EB0, #25AEB4 )',
    color: '#fff',
    fontWeight: 600,
    padding: '10px 15px'
})
export default MuButton;


